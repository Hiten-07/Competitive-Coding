LCA(root, n1, n2):

if root == NULL:
    return NULL

if root == n1 OR root == n2:
    return root

left = LCA(root->left)
right = LCA(root->right)

if left != NULL AND right != NULL:
    return root

if left != NULL:
    return left

return right
